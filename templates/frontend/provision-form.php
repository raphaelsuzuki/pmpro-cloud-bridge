<?php
/**
 * Template: Deploy new server form.
 *
 * Shown inside the member dashboard when the user has remaining quota.
 * Full template delivered in the Frontend phase.
 *
 * @package CloudBridge
 */

declare(strict_types=1);

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
