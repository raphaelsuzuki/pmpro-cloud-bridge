<?php
/**
 * CheckoutHooks — decorates PMPro checkout for cloud-level plans.
 *
 * Full implementation delivered in the Frontend phase.
 *
 * @package CloudBridge\Frontend
 */

declare(strict_types=1);

namespace CloudBridge\Frontend;

/**
 * Adds region/image/hostname fields to the PMPro checkout for cloud levels.
 * Validates them server-side before checkout proceeds.
 */
final class CheckoutHooks {
	// Full implementation delivered in Frontend phase.
}
